title: "P(doom) Calculator"
description: "Estimate the probability that AI causes a global catastrophe, one link of the chain at a time."
url: "https://neoneye.github.io"
baseurl: "/pdoom-calculator"

defaults:
  - scope:
      path: ""
    values:
      layout: default
